<UserControl x:Class="Cook_Book_Client_Desktop.Views.RecipesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cook_Book_Client_Desktop.Views"

             mc:Ignorable="d"  Background="White" FontSize="24"
             d:DesignHeight="450" d:DesignWidth="800"
             
              xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:cal="http://www.caliburnproject.org"
             
             >
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>


        <TextBlock Grid.Row="0" 
                   HorizontalAlignment="Center" FontSize="55" FontFamily="Segoe UI Light"
                   Margin="0 0 0 20">
            Twoje przepisy
        </TextBlock>
        <ListBox x:Name="Recipes" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Text="{Binding Name}" FontSize="15" Margin="0 0 10 0"/>
                        <TextBlock Text="{Binding Instruction}" FontSize="12"/>
                        <Button FontSize="15" cal:Message.Attach="EditRecipe($dataContext)">
                            Edytuj
                        </Button>
                        <Button FontSize="15" cal:Message.Attach="DeleteRecipe($dataContext)">
                            Usuń
                        </Button>
                    </WrapPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <DockPanel Grid.Row="2">
            <Button x:Name="AddRecipe" DockPanel.Dock="Bottom"  Margin="0 10 0 0" FontSize="36" Padding="0 10 0 10">
                Dodaj nowy przepis!
            </Button>
        </DockPanel>

    </Grid>
</UserControl>
